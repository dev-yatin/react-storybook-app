import { Primary } from "../Button/Button.stories";
import HCenter from "../HCenter/HCenter";
import { Large } from "../Input/Input.stories";

export default {
  title: "Subscription",
};

// Used stories inside stories
export const Subscription = () => {
  return (
    <>
      <Large />
      <Primary />
    </>
  );
};
